import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    featuredContent: {
        // marginVertical: 10,
      },
      featuredImage: {
        width: "100%",
        height: 200,
      },
})

export default styles;